<div class="container">
    <div class="details">
      <h2>Dime Algo:</h2>
      <% if @post.errors.any? %>
        <% @post.errors.full_messages.each do |error| %>
          <p class="alert alert-danger"><strong>Input Error: <%= error %></strong></p>
        <% end %>
      <% end %>

      <%= form_for @post do |f| %>

        <div class="form-group">
          <%= f.label :text %>
          <%= f.text_field :text, class: "form-control" %>
        </div>

        <div class="">
          <%= f.submit "¡Tweetalo, Guey!", class: "btn btn-primary" %>
        </div>

      <% end %>
    </div>
</div>
